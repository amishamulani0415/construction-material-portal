# Construction Material Request Portal — Frontend (React + Vite)

## Quick Start
```bash
cd frontend
npm install
npm run dev
```
By default it expects backend at `http://localhost:4000`. To override, create `.env` with:
```
VITE_API=http://localhost:4000
```
